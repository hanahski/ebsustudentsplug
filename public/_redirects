# Consolidate www to the bare domain. Do NOT redirect *.lovable.app —
# Vercel proxies /_serverFn/* and /api/* to ebsustudentsplug.lovable.app,
# so that host must keep serving normally.
https://www.ebsustudentsplug.fun/* https://ebsustudentsplug.fun/:splat 301!
